apiVersion: apps/v1
kind: Deployment
metadata:
  name: ui
spec:
  selector:
    matchLabels:
      app: ui
      tier: frontend
  template:
    spec:
      imagePullSecrets:
      - name: dockerconfigjson-github-com
      containers:
        - name: ui
          env:
          - name: ASPNETCORE_ENVIRONMENT
            value: Production
          - name: ASPNETCORE_HOSTINGSTARTUPASSEMBLIES
            value: Microsoft.AspNetCore.SpaProxy
          - name: ASPNETCORE_URLS
            value: http://+
          - name: Budgeting__Url
            value: http://budgeting-prd
          - name: ObjectStore__Url
            value: http://object-store-prd
          - name: AngularSettings__RedirectUrl
            value: https://holefeeder.drifterapps.app
