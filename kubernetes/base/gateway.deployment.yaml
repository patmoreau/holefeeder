apiVersion: v1
kind: Service
metadata:
  name: gateway
  labels:
    app: gateway
spec:
  ports:
    - port: 80
  selector:
    app: gateway
    tier: backend
  type: ClusterIP
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: gateway
  labels:
    app: gateway
spec:
  selector:
    matchLabels:
      app: gateway
      tier: backend
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app: gateway
        tier: backend
    spec:
      containers:
      - image: holefeeder/holefeeder-gateway-web
        name: gateway
        ports:
        - containerPort: 80
          name: gateway
        resources:
          requests:
            memory: 32Mi
            cpu: 10m
          limits:
            memory: 64Mi
            cpu: 50m
