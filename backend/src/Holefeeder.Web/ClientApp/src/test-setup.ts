// test-setup.ts
import 'zone.js/testing'; // Required for Angular testing
import { TestBed } from '@angular/core/testing';

beforeEach(() => {
  TestBed.configureTestingModule({
    // Your other testing module configurations
  });
});

// Import your testing modules here if needed
// e.g., import './app/testing.module';
