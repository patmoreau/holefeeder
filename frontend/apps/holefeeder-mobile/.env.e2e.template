# Copy to .env.e2e.local and fill in real values. Never commit .env.e2e.local.
#
# Auth0 dev-tenant user that is already registered in the backend. Used by the
# flows tagged `auth`, which drive the real Universal Login pages.
E2E_EMAIL=replace-me@example.com
E2E_PASSWORD=replace-me
